#include <vector>

#include <XAD/XAD.hpp>
#include <XAD/StdCompatibility.hpp>

template <typename T>
std::vector<void (*)> &make_functions()
{
    return std::vector<void (*)>({
            *abs,
            *acos,
            *acosh,
            *asin,
            *asinh,
            *atan,
            *atan2,
            *atanh,
            *cbrt,
            *ceil,
            *copysign,
            *cos,
            *cosh,
            *erf,
            *erfc,
            *exp,
            *exp2,
            *expm1,
            *fabs,
            *floor,
            *fmax,
            *fmin,
            *fmod,
            *fpclassify,
            *frexp,
            *hypot,
            *ilogb,
            *isfinite,
            *isinf,
            *isnan,
            *isnormal,
            *ldexp,
            *llround,
            *log,
            *log10,
            *log1p,
            *log2,
            *lround,
            *max,
            *min,
            *modf,
            *nextafter,
            *pow,
            *remainder,
            *remquo,
            *round,
            *scalbn,
            *signbit,
            *sin,
            *sinh,
            *sqrt,
            *tan,
            *tanh,
            *trunc,
    }),
}